{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${author}"
  ],
  "contact": {
    "homepage": "https://rhm176.de",
    "issues": "https://github.com/Raik176/mod-credits-reborn/issues",
    "sources": "https://github.com/Raik176/mod-credits-reborn"
  },
  "license": "${license}",
  "icon": "assets/${id}/icon.png",
  "environment": "client",
  "entrypoints": {
    "main": [
      "${group}.${id}.fabric.ModCreditsFabric"
    ]
  },
  "mixins": [
    "${id}-common.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.15",
    "minecraft": "${minecraft}"
  }
}